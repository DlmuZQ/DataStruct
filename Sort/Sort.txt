1、冒泡排序：
   从区间0~N-1到0~1：前后两个数进行比较，将大的数放在后面，完成之后整个数组变得有序。
2、选择排序：
   从区间0~N-1到N-1：选择数字最小的数放在区间开始位置，完成之后整个数组变得有序。
3、插入排序：
   从位置2开始到N-1：与前面的数相比较，如小于前面的数则交换位置，直到大于或相等，完成之后整个数组有序。
4、特殊的插入排序（不稳定排序）
5、堆排序：
   将待排序数组构建成大顶堆，将堆顶移走（与最后一个元素交换位置）,将剩下的部分重新构建大顶堆。